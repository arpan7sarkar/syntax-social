const {authRouter}=require('./auth.route')
const {profileRouter}=require('./profile.route')
const {connectionRouter}=require('./connection.route')

module.exports={authRouter,profileRouter,connectionRouter}